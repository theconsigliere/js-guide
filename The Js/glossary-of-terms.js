//Scroll position = window.scrollY
//the height of the browser window's viewport. = window.innerHeight
//the pixel level of the bottom of the page = (window.scrollY + window.innerHeight)

//the height of the whole browser window = window.outerHeight.

//
