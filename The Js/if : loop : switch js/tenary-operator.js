  //teneray operator

  
  //simple if statement = tenary operator
  activePlayer === 0 ? activePlayer = 1 : activePlayer = 0;

  // full if version
  if(activePlayer === 0) {
      activePlayer = 1;
  } else {
      activePlayer = 0;
  }
}